---
import Link from "./Link.astro";
import HorizontalContainer from "./HorizontalContainer.astro";

const currentYear = new Date().getFullYear();
---

<footer>
  <HorizontalContainer>
    <div class="pt-12 pb-16">
      <div class="flex items-center justify-between">
        <div class="flex items-center gap-4">
          <strong>AI tools</strong>
          <ul class="flex items-center gap-2">
            {
              ["llms.txt", "llms-small.txt", "llms-full.txt"].map((file) => (
                <li>
                  <a
                    href={`/${file}`}
                    class="text-gray duration-hover hover:text-nix-light-blue dark:hover:text-orange text-sm"
                  >
                    <code>{file}</code>
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
        <div>
          <p
            class="text-gray dark:text-gray tracking-tight md:text-base lg:text-lg"
          >
            Copyright &copy; {currentYear}
            <Link
              href="https://determinate.systems"
              class="duration-hover hover:text-primary dark:hover:text-orange"
            >
              Determinate Systems, Inc.
            </Link>
          </p>
        </div>
      </div>
    </div>
  </HorizontalContainer>
</footer>
