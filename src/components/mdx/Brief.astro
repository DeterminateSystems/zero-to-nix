---
import { getEntry } from "astro:content";

type Props = {
  id: string;
};

const { id } = Astro.props;

const page = await getEntry("briefs", id);

if (page === undefined) {
  throw new Error(`No brief with id ${id} was found`);
}

const { Content } = await page.render();
---

<Content />
