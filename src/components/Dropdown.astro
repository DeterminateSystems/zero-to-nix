---
type Props = {
  text: string;
  pages: { title: string; href: string }[];
};

const { text, pages } = Astro.props;
---

<div class="relative inline-flex" x-data="{ open: false }">
  <button
    @click="open = !open"
    class="duration-hover text-sm tracking-tight hover:text-primary focus:outline-none focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-opacity-75 md:text-base lg:text-lg"
    >{text}</button
  >

  <div
    x-show="open"
    @click.away="open = false"
    x-transition:enter.duration.200ms
    class="absolute right-0 top-10 max-h-[75vh] w-72 origin-top-right overflow-scroll bg-white p-4 shadow-md ring-1 ring-black ring-opacity-5 focus:outline-none dark:border-gray dark:bg-dark"
  >
    <ul class="flex flex-col space-y-1.5">
      {
        pages.map(({ title, href }) => (
          <li>
            <a
              {href}
              class="duration-hover block px-4 py-2 text-xs tracking-tight hover:bg-primary hover:text-dark hover:text-white md:text-sm lg:text-base dark:hover:bg-white dark:hover:text-dark"
            >
              {title}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</div>
