---
import { Icon } from "astro-icon/components";
import {
  getConceptPagesAlphabetical,
  getStartPagesByOrderParam,
} from "../content/collections";
import { conceptPagePath, startPagePath } from "../lib/utils";
import { site } from "../site";
import Drawer from "./Drawer.astro";
import DrawerToggler from "./DrawerToggler.astro";
import Dropdown from "./Dropdown.astro";
import HorizontalContainer from "./HorizontalContainer.astro";
import Link from "./Link.astro";
import ThemeSwitcher from "./ThemeSwitcher.astro";

const { title, githubUrl, navbarLinks } = site;

const startPages = (await getStartPagesByOrderParam()).map(
  ({ id, data: { title } }) => {
    return { title, href: startPagePath(id) };
  },
);

const conceptPages = (await getConceptPagesAlphabetical()).map(
  ({ id, data: { title } }) => {
    return { title, href: conceptPagePath(id) };
  },
);
---

<nav
  class="shadow-light-gray dark:bg-dark dark:shadow-dark-gray sticky top-0 z-10 bg-white py-2 shadow-sm md:py-2.5 lg:py-3"
>
  <HorizontalContainer>
    <div class="flex items-center justify-between">
      {/* Navbar "brand" */}
      <div class="flex items-center space-x-2 md:space-x-3 lg:space-x-4">
        <Link
          href="/"
          class="duration-hover hover:text-primary dark:hover:text-light-gray text-2xl font-semibold tracking-tight md:text-3xl lg:text-4xl"
        >
          {title}
        </Link>

        <span
          class="flex items-center space-x-1.5 text-sm font-light md:text-base lg:text-lg"
        >
          <span>from</span>
          <Link
            href="https://determinate.systems"
            class="duration-hover hover:text-primary dark:hover:text-light-blue font-semibold"
            target="_blank"
          >
            Determinate Systems
          </Link>
        </span>
      </div>

      {/* Visible only on mobile */}
      <div class="flex items-center space-x-4 md:hidden">
        <ThemeSwitcher />

        <Link href={githubUrl} target="_blank">
          <Icon
            name="radix-icons:github-logo"
            class="duration-hover hover:text-gray dark:hover:text-light-gray size-4 transition md:size-5 lg:size-6"
          />
        </Link>

        <DrawerToggler />
      </div>

      {/* Show only on md or above (invisible on mobile) */}
      <ul class="hidden items-center gap-4 md:flex md:gap-5 lg:gap-6">
        <li>
          <Dropdown text="Quick start" pages={startPages} ordered />
        </li>
        <li>
          <Dropdown text="Concepts" pages={conceptPages} />
        </li>

        {
          navbarLinks.map(({ text, href }) => (
            <li>
              <Link
                {href}
                class="duration-hover hover:text-primary text-sm md:text-base lg:text-lg"
              >
                {text}
              </Link>
            </li>
          ))
        }

        <li class="flex items-center space-x-2 md:space-x-3 lg:space-x-4">
          <ThemeSwitcher />

          <Link href={githubUrl} target="_blank">
            <Icon
              name="radix-icons:github-logo"
              class="duration-hover hover:text-gray dark:hover:text-light-gray size-4 md:size-5 lg:size-6"
            />
          </Link>
        </li>
      </ul>
    </div>
  </HorizontalContainer>
</nav>

<Drawer />
