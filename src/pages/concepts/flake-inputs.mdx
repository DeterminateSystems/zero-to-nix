---
title: Flake inputs
snippet: Fill me in later so that I show up in the tooltip
related: ["flake-outputs"]
---

Flake **inputs** are the Nix code that you want to rely on in your current flake.

```nix filename=flake.nix
{
  inputs = {
    nixpkgs = "github:NixOS/nixpkgs";
  };
}
```
