---
layout: ../../layouts/Step.astro
title: Run a program
description: Use Nix to run an executable with no setup or installation
weight: 2
---

The Nix CLI has a [`run`][run] command that you can use to run executables. Here's an example:

```shell
nix run github:DeterminateSystems/riff
```

[run]: https://nixos.org/manual/nix/stable/command-ref/new-cli/nix3-run.html
