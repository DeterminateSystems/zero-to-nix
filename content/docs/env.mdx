---
title: Create a development environment
description: Work on your application inside a hermetically sealed sandbox
weight: 2
---

Select your language below:

<Languages />

To get set up using Nix in your <Language /> project:

<Script />

<Topic id="flake-template" />

Now examine the contents of your directory:

```shell
ls .
```

What you should see:

```shell
flake.lock flake.nix
```

Let's go over what those files do.

* The [`flake.lock`][flake.lock] file <Concept text="pins" id="pinning" /> all of your Nix dependencies to specific [Git revisions][rev]
* The [`flake.nix`][flake.nix] file


[flake.lock]: TODO
[flake.nix]: TODO
[rev]: TODO

