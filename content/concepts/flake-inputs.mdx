---
title: Flake inputs
snippet: Other flakes that the current flake relies on.
---

Flake **inputs** are the Nix code that you want to rely on in your current flake.

```nix
{
  inputs = {
    nixpkgs = "github:NixOS/nixpkgs";
  };
}
```
