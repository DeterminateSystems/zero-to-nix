# Lint rule for "e.g."
# Inspired by Buildkite: https://github.com/buildkite/docs/blob/main/vale/Buildkite/eg.yml
extends: existence
message: Consider replacing '%s' with 'for example' or 'such as'
level: error
ignorecase: true
tokens:
  - '\be\.g\b'
  - '\be\.g.\b'
