# Lint rule for "i.e."
# Inspired by Buildkite: https://github.com/buildkite/docs/blob/main/vale/Buildkite/ie.yml
extends: existence
message: Consider removing '%s' or replacing it with something like 'that is'
level: error
ignorecase: true
tokens:
  - '\bi\.e\b'
  - '\bi\.e.\b'
