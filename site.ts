export type Site = {
  title: string;
  description: string;
  language: string;
};

const site: Site = {
  title: "Zero to Nix",
  description: "Up and running in no time",
  language: "en",
};

export default site;
